# schwaizer-opendata-mcp environment example
# Copy this file to ".env" and adjust values as needed.

# Base CKAN action API endpoint (no trailing slash)
BASE_URL=https://opendata.swiss/api/3/action

# Enable CKAN datastore_search_sql tool (guarded). Defaults to false for safety.
# When false, calling datastore_search_sql will return an error.
ENABLE_SQL=false

# HTTP client timeout in milliseconds
TIMEOUT_MS=15000

# User-Agent header sent to the CKAN API
USER_AGENT=schwaizer-opendata-mcp/0.1.0 (+https://opendata.swiss)

# Row limit clamps for searches (server-side caps still apply)
# MAX_ROWS is the absolute cap the server will allow
# DEFAULT_ROWS is the default when a client does not specify "rows"/"limit"
MAX_ROWS=1000
DEFAULT_ROWS=25
